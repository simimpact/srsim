package simulation

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

// This test does nothing. It is here to test imports to ensure that none are broken. If it fails,
// something is wrong with implementations
func TestPlaceholder(t *testing.T) {
	assert.True(t, true)
}
