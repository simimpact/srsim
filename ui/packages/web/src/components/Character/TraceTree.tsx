import { Path } from "@/bindings/AvatarConfig";

interface Props {
  ascension: number;
  characterId: number;
  path: Path;
}
const TraceTree = ({ ascension, characterId, path }: Props) => {
  return `hello`;
};
export { TraceTree };
